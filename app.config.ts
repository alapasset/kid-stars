// To fix the error, you need to add the nuxtIcon property to the defineAppConfig function in the app.config.ts file.
// https://github.com/nuxt-modules/icon/issues/117
export default defineAppConfig({
    nuxtIcon: {}
})